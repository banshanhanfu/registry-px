# registry 升级完善盘点（2026-09-24 · 基线 m197）

> 全仓 55 个库（0.1.0 单版本）普查：规划文档（library-plan-go/python/rust）里的库**全部落地**，
> 无"计划未建"。以下按可升级维度分组，状态随实施更新（✅ 完成 / ⏳ 待做 / 🚫 不可行）。
> 判定依据：m197 实测 + 官方 PX_DEF_TRIAGE / CHANGELOG（M184–M189 语言面修复）。

## A. 语言缺陷已修复、库内残留过时 workaround/注释（低风险清理）

| 库 | 位置 | 现状 | m197 已修 | 状态 |
|---|---|---|---|---|
| bytes_pack | 头部实现说明 | "规避 PX-DEF-019/020" 过时；自实现补码转换可收敛到 `int_to_bytes`/`bytes_to_int` | M186 | ✅ |
| strcase | 头部说明 | "native 无 ord，见 PX-DEF-013" — ord 已存在；PX-DEF-013 是对方跳号、不存在 | ord 在 m197 | ✅ |
| checksum | 头部实现说明 | "切片按字节（PX-DEF-015）" 过时（M185 三面统一 rune） | M185 | ✅ |
| ini | 头部说明 | "字符串切片按字节（PX-DEF-015 语义）" 过时，需复核 | M185 | ✅ |
| fractions | 头部说明 | "不同于 int() 宽容前缀解析" 过时（M184 已严格化） | M184 | ✅ |
| base58 | `b58_double_sha256_hex` | 用 `hex_to_bytes(sha256(...))` 绕行双哈希 | M188 `sha256_bytes` | ✅ |
| parser | p_alt 138 行 | "Result 无 unwrap_err，用 str(Err(...)) 剥壳" | M185 `unwrap_err` | ✅ |

## B. 声明的 0.1.0 子集缺口、原生已就绪（实质功能升级 → 0.2.0）

| 库 | 缺口 | 可行性 | 状态 |
|---|---|---|---|
| bytes_pack | "不支持 float / 对齐填充（待 native 支持后 0.2.0 加）" | ✅ `bits_to_float32/64` + `float32_bits/float64_bits` 已实测往返一致 | ✅ |
| base58 | 双哈希绕行 | ✅ `sha256_bytes`（与 A 同项） | ✅ |
| jwt | HS256 子集 | ⚠️ 可加 RS256（`rsa_sign_pkcs1v15_sha256` 等原生均在，需补 PEM/DER 解析）；HS384/512 暂不可行（无 hmac_sha384/512） | ⏳ |
| mailparse | 不解码 RFC 2047 编码字 / MIME 主体 | ✅ base64_decode 已就绪 | ⏳ |
| secure_random | 严格均匀需拒绝采样，0.1.0 未实现 | ✅ 可补拒绝采样循环 | ⏳ |
| idcard | 不做 15 位旧号、地区码枚举表 | ✅ 纯数据表 + 校验位逻辑 | ⏳ |
| big | 不支持十六进制/科学计数法 | ✅ 可加 hex 输入输出 | ⏳ |
| ipaddr | 不支持内嵌 IPv4 尾段 | ✅ 可做 | ⏳ |
| cli / validator / template / table / datetime / diff | 各 0.1.0 子集 | 视需要扩展 | ⏳ |
| mysql | caching_sha2 full auth（MySQL 8 默认）需 TLS/RSA | ⚠️ 大项（pg 有 tls_upgrade 先例） | ⏳ |

## C. 本环境无法解决（已登记，不排期）

- `xlsx`：aarch64 解释轨缺 zip 族（PX-DEF-021 平台差异）
- `workerpool` / `concurrent_map` / `rate`：并发需编译模式（PX-DEF-006 设计性）
- `qrcode`：解释器性能（PX-DEF-024）

## D. 工程基建（非库本身）

- `dotenv`/`glob` 测试依赖 `/tmp` 手工 fixture — 可加 `tests/fixtures.sh` 一键预置
- 仓库无 CI — 可加 GitHub Actions 跑 `px run` 全量普查

## 实施记录

- 2026-09-24：盘点建档（commit docs）。
- 2026-09-24：**A 类 7 库全部完成** —— bytes_pack（收敛原生 int_to_bytes/bytes_to_int，PX-DEF-019/020 注释更新）、strcase/checksum/ini/fractions（过时缺陷注释修正）、base58（双哈希 → 原生 sha256_bytes）、parser（unwrap_err 取代 str(Err()) 剥壳）。
- 2026-09-24：**B 类 bytes_pack 完成** —— 新增 `f`/`d`（float32/float64）类型符，走 float32_bits/float64_bits + bits_to_float32/64，对拍 Python struct 逐字节一致；base58 的 sha256_bytes 见 A。
- 待办：B 类其余（jwt RS256 需补 PEM/DER 解析、mailparse RFC2047、secure_random 拒绝采样、idcard 15 位/地区码、big hex、ipaddr 内嵌 IPv4、mysql caching_sha2 full auth 等）。