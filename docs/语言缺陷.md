# 语言缺陷（PuXian Language Defects）

> 本仓库在编写生态库过程中暴露的 PuXian 语言缺陷/不直观行为登记。
> **约定**：写库时遇到任何语言面问题（编译/解释不一致、原生函数语义不直观、
> 语法限制、文档与实现不符等），一律追加到本文件（新条目编号 = 当前最大 ID + 1），
> 随当次库的 commit 一起提交。修复/官方回应后可标记状态。
>
> 实测基线：PuXian 0.2.0-m206（aarch64，官方引导包；VM 轨件仅 x86_64 侧产出 ⇒ 自动落 C 轨）
> （2026-09-25 按 m206 复查）：
> - m184 已移除 PX-DEF-002/005/014（官方缺陷 200/201/202）；
> - M185–M189 官方 release 已处理并移除：PX-DEF-003（M185 新增 `unwrap_err`）、
>   018/019/020（M186 解释轨 native 透传收口）、025/026/027（M188 `.find`/`sha1`/方法面别名）、
>   012/016/017（M188 新增 `bytes_zeros`/`sha256_bytes`/`byte` 安全替代）、
>   029（M189 `bytes_get` 越界统一报 R1003）、015（M185 复测否定，三面已统一为 rune）；
> - **M201 已处理**：PX-DEF-021/035（解释器件能力面收口：`native_symbols()` + pxi 构建 `--full`，
>   aarch64 引导包解释轨 zip/zlib 整族恢复）、031（官方判定 = 8 字节回绕保留为设计 +
>   新增 `bytes_to_dec` 精确十进制 + `bytes_to_int` 越界 null 收口 R1003）；
> - **M202 已处理**：PX-DEF-032（`base32_encode`/`base32_decode`/`base32_to_bytes`）、
>   033（`hmac_sha1`/`hmac_sha1_bytes`）、034（`sorted(xs[, key_fn])`；comparator 明确不做）；
>   —— 以上 6 条均已在 **m204 aarch64 引导包**首次实测（解释轨/编译轨一致），**m206 复查保持**；
> - **M205/M206 复查**（2026-09-25）：无新处置；PX-DEF-036/037 经官方速查表确认**早有记载**（事实：
>   `bytes_slice`/`b[a:b]` 负边界越界一律 clamp、`start>end` ⇒ 空；§9 与事实 14/32/50/62 保留字坑），
>   状态列改「文档已补（行为不变）」；
> - 其余登记为官方判定的设计/文档类（状态列已更新）：006/007/022/023/024/028 设计如此，
>   008/009/010/030 官方文档已补（行为不变）。

## 缺陷登记

| ID | 发现库 | 描述 | 影响 | 绕行 | 状态 |
|---|---|---|---|---|---|
| PX-DEF-006 | workerpool/testkit | **解释器（`px run`）不支持并发原语**（spawn/send/recv/select/chan/mutex）→ 报"interp 不支持通道（Mini 子集排除）" | 含并发库无法用解释器验证（文档化行为，非缺陷，但写库易踩） | 并发库标注"需编译模式"，测试用 `px build` + 产物运行 | 官方判定**设计如此**（MINI_SUBSET 排除）；速查表已写明 |
| PX-DEF-007 | workerpool | **函数值作为 chan 消息元素传参可用，但注意任务 dict 键访问必须 `.has()` 守卫**：对无键 dict 直接 `d["k"]` 抛 `R1008 字典没有键`（而非返回 null/报可捕获错误） | 宽松读键模式（Python `.get` 心智）不可用 | `task.has("stop") and task["stop"]` 式守卫；或先 `d.keys()` 确认 | 官方判定**设计如此**（响亮优于静默）；速查表已写明（先守卫再读） |
| PX-DEF-008 | validator | **`type()` 对字符串返回 `"string"` 而非 `"str"`**：`type("")` 返回 `"string"`（速查表未记录 str 的实际类型名；`type(1)` 是 `"int"`、`type([])` 是 `"list"` 等均与直觉一致） | 按 `type(v) == "string"` 判断字符串，勿用 `"str"` | 文档未覆盖，已实测 | 官方**文档已补**（速查表记录实际类型名为 `string`）；行为不变，写库仍需注意 |
| PX-DEF-009 | validator | **`regex_match(pattern, s)` 是完整匹配而非搜索**：`regex_match("a@b", "xa@by")` 返回 false（Go regexp.MatchString 语义是搜索） | 跨实现移植正则断言时需注意；需要子串匹配时 pattern 两端不要隐含 `^$` | 文档未明示，已实测 | 官方**文档已补**（`regex_match` 完整匹配，`regex_search` 为搜索语义）；行为不变 |
| PX-DEF-010 | 写库综合 | **`pbkdf2_sha256` 的 salt 参数按字符串字节处理，不自动 hex 解码**：`pbkdf2_sha256(pw, "aabb…")` 把 32 字符 ASCII 当 32 字节盐（与 Python `hashlib.pbkdf2_hmac` 传入 `bytes.fromhex` 不同）。传 `hex_to_bytes(...)`（bytes）时与标准**完全一致**（实测 64 字节输出逐字节相同） | 与标准互操作必须显式 hex_to_bytes | native 入参双形态（str/bytes）易误读 | 官方**文档已补**（salt 按字符串字节处理）；行为不变，互操作须 `hex_to_bytes` |
| PX-DEF-021 | xlsx | **`zip_pack`/`zip_unpack` 在 aarch64 官方引导包的解释轨缺失**：`zip_pack` 调用报 `R1001 未定义变量`，仅编译轨（`px build`）可用（m197 实测；`gzip_compress` 等同族在解释轨正常） | zip 相关库（xlsx/docx/pptx 容器）无法用本机解释轨验证 | 该库标注"需编译模式"；纯 parts 构建在解释轨仍可测 | ✅ 已修复（M201 解释器件能力面收口：pxi 构建 `--full`；m204 aarch64 引导包解释轨实测 `zip_pack`/`zip_unpack` 在场且真调用生成文件） |
| PX-DEF-022 | qrcode | **list 切片是浅拷贝（内层元素共享）**：`m[0:len(m)]` 得到新外层数组，但内层 list 元素仍是同一引用；`var b = a` 更是纯引用。需要整块深拷贝必须手写逐元素循环（每库重复实现 `copy_m`/`list_copy` helper），无 `deepcopy` 原语 | 矩阵/嵌套列表复制易串改原值，边界难查 | 手写双层循环深拷贝；或构建期约束"导出函数不改入参" | 官方判定**设计如此**（与 Python/Go 浅拷贝语义一致）；速查表已写明 |
| PX-DEF-023 | qrcode | **`Err(...)` 字符串化嵌套冗长**：`str(Err("a: " + str(Err("b"))))` 输出 `Err(a: Err(b))` 双层包裹，日志/错误传播链路难以阅读 | 多层错误传播时排障信息膨胀 | 顶层捕获后只 `str(err)` 一次并人工剥壳；库内部避免 Err 套 Err | 官方判定**设计如此**（展示形态）；速查表已写明 |
| PX-DEF-024 | qrcode | **解释器循环性能极差**：QR 掩码罚分全矩阵大循环（8 掩码 × O(n²)）`px run` 单码约 36s，`px build` 编译产物 <1s（同源码实测） | 含大循环/多重循环的库无法用解释器做快速迭代验收 | 写库先用 `px build` 编译产物验收；解释器只做小样例冒烟 | 官方判定**设计性**（解释器非性能轨）；速查表已写明 |
| PX-DEF-028 | pg/mysql | **C 风格 `for (;;)` 不支持**：报 `E1001 非法字符: ;`；无限循环只能 `while true:` | 从 C/Go 惯例直接写 `for(;;)` 卡语法错误 | 统一 `while true:` + 显式条件退出 | 官方判定**设计如此**（语法风格）；速查表已写明 |
| PX-DEF-030 | pg | **`bytes_to_int` 读 `0xFFFFFFFF` 返回无符号 4294967295（非 -1）**：PG DataRow 的 NULL 列长度 -1 无法用 `== -1` 判断 | 二进制协议负长度判定错误 → NULL 列解析成数据 | 显式传 `bytes_to_int(b, "big", true)`（`signed` 形参一直存在，即得 -1）；或 `> 2147483647` 判负 | 官方确认 **`signed` 形参一直存在**（速查表事实 221 已补）；文档缺口已补，行为不变 |
| PX-DEF-031 | mysql（写二进制协议） | **`bytes_to_int` 读 8 字节返回 `-1`（静默 wrap 为负）**：`bytes_to_int(hex_to_bytes("ffffffffffffffff"), "little", false)` 返回 **-1**（应为 18446744073709551615）；`0x8000000000000000`（2^63）返回 **-9223372036854775808**——三轨一致但**静默给错值**，且 `int` 是 64 位有符号，无法直接表示 u64 | 二进制协议解析 `BIGINT UNSIGNED` 列（>2^63-1）会静默得到负数 | 本库 `my_stmt_value` typ==8 无符号且 <0 时走 `my_u64_dec`（32 位高低字长除法 → 十进制字符串）；官方宜考虑 `bytes_to_uint64` 或以字符串/错误形式返回 | ✅ 已修复（M201 官方判定：8 字节 unsigned 回绕保留 = Go `int64(uint64)` 语义，属设计，msgpack/mysql 依赖；真缺口已补 `bytes_to_dec` 精确十进制；`bytes_to_int` 越界 null 收口 R1003） |
| PX-DEF-032 | totp（写 TOTP） | **无 `base32` native**：编码族 base64/hex 原生齐全，但 RFC 4648 base32（OTP 秘钥标准格式、Google Authenticator 互通必需）缺失；`base32_encode`/`base32_decode` 均 `R1001 未定义变量`（runtime + stdlib 均无） | TOTP/HOTP 秘钥（Google Authenticator 兼容）只能用 base32 文本交换，无法原生编解码 | 本库纯 .px 自实现 RFC 4648 解码（`to_upper` + 5bit 累积）；官方宜补 `base32_encode/decode` 三轨原生 | ✅ 已修复（M202 `base32_encode`/`base32_decode`/`bytes_base32`/`base32_to_bytes`，RFC 4648 §10 全向量；m204 实测往返一致） |
| PX-DEF-033 | totp（写 TOTP） | **无 `hmac_sha1` native**：HMAC 族只有 `hmac_sha256`；RFC 4226/6238 的默认算法是 HMAC-SHA1（Google Authenticator 只认 SHA1） | HOTP/TOTP 标准实现只能退而用 SHA256 或自拼 HMAC | 本库用 `sha1_bytes` 纯 .px 拼 HMAC（RFC 2104，64 字节块 + 0x36/0x5c）；官方宜补 `hmac_sha1`/`hmac_sha1_bytes` | ✅ 已修复（M202 `hmac_sha1`/`hmac_sha1_bytes`，RFC 2202 §3 全向量；m204 实测命中） |
| PX-DEF-034 | natsort（写自然排序） | **`sorted` 只接受 1 个参数（无 key/comparator）**：`sorted(l, fn)` 报 `R1002: sorted 需要 1 个参数`（Python `sorted(key=)` / Go `sort.Slice` / Rust `sort_by` 皆有自定义比较） | 自然排序等按自定义键排序无原生入口，且原生排序的稳定保证无法直接复用于自实现 | 绕行：[键, 原始下标, 原值] 三元组成对包装 → 原生 `sorted` 复合比较（M162 稳定）→ 取原值（natsort.px::na_sorted）；官方宜加 `sorted_keyed(l, key)` 或比较器参数 | ✅ 已修复（M202 `sorted(xs[, key_fn])`：取键恰一次/稳定/无 key 零回归；comparator 官方明确不做） |
| PX-DEF-037 | sched（写调度器） | **`fn` 是匿名函数关键字，不能用作标识符**：作为函数参数名/局部变量名一律语法错误 E2001（0.1.0 实测：`def f(x, fn)` 报「期望变量名，实际得到 fn」、`t.set("fn", fn)` 报「期望 '('」，得到的 ')'」）；官方速查表早期已记载（§9/事实 14/32/50/62） | 写库时回调参数撞名 | 回调参数/局部变量用 `cb`/`f` 等命名；勿用 fn/cb_xxx 前缀回避 | 官方速查表已记载（§9 与事实 14/32/50/62：`fn` 是保留字不可作标识符）；m206 行为不变，回调参数命名用 `cb`/`f` |
| PX-DEF-036 | dns（写 UDP DNS 客户端） | **`bytes_slice(b, start, end)` 是半开区间 `[start, end)` 语义**：第三个参数是「结束索引」而非「长度」（与 Go/列表切片 `a[i:j]` 一致）；从 Python `bytes[start:start+len]` 心智写成「起点+长度」会得到空切片/截断（0.1.0 实测：`bytes_slice(b, 1, 3)` 取到的是 `b[1:3]` 共 2 字节） | 二进制协议解析按 `end = off + n` 传值；`bytes_slice` 官方速查表已记载 clamp 语义 | 解析代码统一按 `[start,end)` 写（如 `bytes_slice(pkt, off, off + 2)` 读 u16） | 官方速查表已记载（`bytes_slice`/`b[a:b]` 负边界越界一律 clamp、`start>end` ⇒ 空）；m206 行为不变，写解析仍按 `[start,end)` |
| PX-DEF-035 | zlib（用官方 C-FFI 写压缩库） | **解释轨 pxi 的 FFI 表缺 zlib 族**：`import "c/zlib"` + `extern def zlib_compress/uncompress/crc32` 在编译轨全通，解释轨调用返回 `Err(ffi_call: 未注册函数: zlib_compress)` —— 双轨分叉（aarch64 m197 引导包 pxi 未带 zlib FFI 表；与 PX-DEF-021 zip 族同族） | 官方 C-FFI（M42 extern def）声称双模式统一走同一 C 桥，但引导包解释器实际缺部分 C 库注册 | 压缩库标注"编译轨可用"；解释轨调用返回可辨 Err 不崩溃（zlib.px 已优雅处理）；官方需让 aarch64 pxi 带全 FFI 表，并补一个"FFI 可用符号清单"查询原语 | ✅ 已修复（M201 `native_symbols()` 可查询能力面 + 解释器件构建 `--full`；m204 aarch64 解释轨实测 zlib FFI 真调用通） |

## 待官方补 native 的登记项（建议签名与 C 绑定草案路径 · 2026-09-24）

> 以下为 registry 侧写的库依赖但**语言层缺 native** 的能力。已按"官方 C-FFI（M42 extern def）"
> 口径给出**建议签名**（对齐既有命名：`hmac_sha256`、`sha256_bytes` 等），官方采纳时照
> PX_DEF_TRIAGE 流程（三轨同做 + 配门 + 负控 + 重定基），并补 FFI 表一致性（防 PX-DEF-035）。
> **进展（2026-09-25）**：PX-DEF-032/033 已被 **M202** 以原生实现采纳（下表前两行已收口）；其余项仍待官方。
> 绑定草案样式参考装件 `runtime/runtime_zlib.c`（绑定函数 + `px_ffi_register` 一行 + 头注释签名）。

| 项 | 关联登记 | 建议签名 | 绑定草案建议 | 备注 |
|---|---|---|---|---|
| base32 编解码 | PX-DEF-032 | `base32_encode(data: bytes) -> str` / `base32_decode(s: str) -> bytes`（RFC 4648，含 padding） | 新 runtime_ffi_base32.c + 注册（~60 行 C） | ✅ 官方 **M202** 已原生实现（`base32_encode`/`base32_decode`/`base32_to_bytes`）；totp 可收敛 |
| HMAC-SHA1 | PX-DEF-033 | `hmac_sha1(key: bytes, msg: bytes) -> str` / `hmac_sha1_bytes(...) -> bytes`（对齐 hmac_sha256 口径） | runtime 加密族文件 + 注册（RFC 2104，~30 行） | ✅ 官方 **M202** 已原生实现（`hmac_sha1`/`hmac_sha1_bytes`）；✅ 官方 **M202** 已原生实现（`hmac_sha1`/`hmac_sha1_bytes`）；同上 |
| zstd / lz4 / brotli / bzip2 / xz 压缩族 | （类库缺口-压缩） | `zstd_compress(data, level) -> bytes` / `zstd_uncompress(data) -> bytes`（各算法同形） | 新 runtime_ffi_zstd.c 等；**装件无雏形，需官方引入三方库/内嵌实现** | zlib 已走 C-FFI 通道可用（编译轨，见 PX-DEF-035） |
| bcrypt / argon2 / scrypt 密码哈希 | （类库缺口-密码） | `bcrypt_hash(pw: str, cost: int) -> str` / `bcrypt_verify(pw: str, hash: str) -> bool`（argon2 同形） | 新绑定 + 算法实现（或复用 libcrypt/三方源码） | passhash 现 PBKDF2，官方补后可换默认 |
| 时区数据（tzdata） | （类库缺口-时间） | `time_zone_load(name: str) -> 句柄` / `time_in_zone(sec, tz) -> "YYYY-MM-DD HH:MM:SS"` | 需数据面（IANA tzdata 子集/数据文件随包分发） | 建议先给"UTC 偏移 + 常见都市"最小集 |
| X.509 证书 | （类库缺口-网络） | `x509_parse_der(der: bytes) -> dict` / `x509_verify_chain(...)` | DER 解析（~300 行）+ 复用 rsa/ed25519 验签原生 | 大件；自签/客户端证书场景常用 |

## 写库时发现的其它限制（非缺陷，设计如此）

- 并发语法（spawn/chan/mutex）仅编译模式（见 PX-DEF-006）
- `let` 不可变，`var` 可变；`{}` 从 M129 起是空 dict（此前是 null）
- `join(sep, list)` **分隔符在前**（与 split 相反，易错）
- `time_format(sec, fmt)` 输出 UTC，本地时间用 `now()`
- dict 键限定 str；`d.set(k,v)` 写、`d.has(k)` 查
- `json_parse_opt` 解析外部 JSON（裸 `json_parse` 遇畸形输入会终止进程）
- 模块顶层不写 `let/var` 状态（写库规范）；有状态用显式传参/dict
- `int_to_bytes(v, size, endian, true)` 对超出**有符号** size 范围的值（如 size=4 且 v>2^31-1）返回 `null`（响亮越界，不截断）；编码负数 float 的 f32 位模式等场景须按无符号 `false` 处理

## 缺陷复现示例


```px
# PX-DEF-022 复现：list 切片浅拷贝（内层共享）
var m = [[1, 2], [3, 4]]
var cp = m[0:len(m)]
cp[0][0] = 99
print(m)   # [[99,2],[3,4]] —— 内层被改了！需手写深拷贝
```

```px
# PX-DEF-023 复现：Err 嵌套字符串化
print(Err("a: " + str(Err("b"))))   # Err(a: Err(b))
```

```px
# PX-DEF-024 复现：解释器与编译产物速度悬殊（qrcode encode 大矩阵罚分）
# px run  : ≈36s（掩码 8 × 罚分 O(n²)）
# px build产物: <1s（同源码）
```

```px
# PX-DEF-028 复现：C 风格 for(;;) 语法不支持
for (;;) {}
# E1001: 非法字符: ';'
# 无限循环须写 while true:
```

```px
# PX-DEF-030 复现：bytes_to_int 读 0xFFFFFFFF 返回无符号 4294967295（非 -1）
# 双模式一致但读 wire protocol 的 -1（NULL 长度）不可用 == -1 判断，
# 需显式传 signed=true（bytes_to_int(b, "big", true) 即得 -1），或 > 2147483647 判负
```
```px
# PX-DEF-031 复现：bytes_to_int 读 8 字节（>2^63-1）静默 wrap 为负【m204 已修复：回绕为官方设计，需精确十进制用 bytes_to_dec】
print(bytes_to_int(hex_to_bytes("ffffffffffffffff"), "little", false))   # -1（应为 18446744073709551615）
print(bytes_to_int(hex_to_bytes("0000000000000080"), "little", false))   # -9223372036854775808（应为 9223372036854775808）
# 绕行：32 位高低字长除法转十进制字符串（本仓 mysql/stmt.px::my_u64_dec）
```
```px
# PX-DEF-032/033 复现：base32 / hmac_sha1 缺失
print(base32_encode("abc"))      # m204 已修复（M202）：返回 MFRGG===
print(hmac_sha1(b"k", b"m"))     # m204 已修复（M202）：返回 40 位 hex
# 绕行：totp.px::o_b32_decode（RFC 4648 纯 .px）与 o_hmac_sha1（sha1_bytes 拼 HMAC）
```
```px
# PX-DEF-034 复现：sorted 无 key/comparator
var cmp = fn(a, b): return a > b   # 匿名函数可用
print(sorted([3, 1, 2], fn(x): 0 - x))  # m204 已修复（M202）：返回 [3, 2, 1]
# 绕行：natsort.px::na_sorted —— [键,下标,原值] 成对包装后走原生 sorted
```
```px
# PX-DEF-035 复现：解释轨 FFI 表缺 zlib（编译轨同脚本正常）
import "c/zlib"
extern def zlib_crc32(data) -> int
print(zlib_crc32(b"hello"))   # m204 已修复（M201）：解释轨同编译轨返回 907060870
```



